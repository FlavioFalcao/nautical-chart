$page.setTitle("$projectName")

<ul>
#foreach ($version in $versions)
  #set($url = "$uiLink.setTarget('ui/version')" + "?project=$projectName&version=$version.getName()")
  <li><a href="$url">$version.getName()</a></li>
#end
</ul>
